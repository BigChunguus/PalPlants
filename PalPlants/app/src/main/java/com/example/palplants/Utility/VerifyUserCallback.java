package com.example.palplants.Utility;

public interface VerifyUserCallback {
    // Método para informar sobre el resultado de la verificación de usuario
    void onVerifyUserResult(boolean result);
}
